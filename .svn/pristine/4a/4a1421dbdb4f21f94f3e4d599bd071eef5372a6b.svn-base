const events = {};

function l(a, b) {
    b.properties.url && $(a).click(function() {
        let a = b.properties.url;
        // window.open(a)
        location.href = a;
    })
}

events["4"] = function(a, b) {
     $(a).attr("data-event", "1120601");
     l(a, b)
};

export default events;